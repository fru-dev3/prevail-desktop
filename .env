# PostHog product analytics (anonymous, opt-in, default OFF). This is the
# PROJECT TOKEN — a write-only key that is safe to embed in client apps; it
# can only send events, not read data. Telemetry stays inert unless the user
# turns it on in Settings → Privacy & telemetry. (See docs/TELEMETRY-PLAN.md.)
VITE_POSTHOG_KEY=phc_oY4UKWHJpQRQZEza8MYbGpiRqFMSPKpNZVF4zvVBuYeA
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Sentry crash reporting (opt-in, default OFF). The DSN is a public client key —
# safe to embed; it can only INGEST events, not read them. Crash reporting stays
# inert unless the user turns it on in Settings → Privacy & telemetry. We init
# with sendDefaultPii=false, no IP, and a beforeSend scrubber (see telemetry.ts).
VITE_SENTRY_DSN=https://a2ef1fe2a597d1025b265ce497111a37@o4511564887490560.ingest.us.sentry.io/4511566459109376
