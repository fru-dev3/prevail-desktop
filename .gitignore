# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# Lock file kept (npm + cargo)
# but tauri's generated schemas can drift

# Bundled engine sidecar — built by scripts/prepare-sidecar.sh
src-tauri/binaries/

# Private planning + launch material — local only, never publish
docs/
launch/
